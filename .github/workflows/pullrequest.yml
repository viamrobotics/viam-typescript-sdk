name: Pull Request Update

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  test:
    uses: viamrobotics/viam-typescript-sdk/.github/workflows/test.yml@RSDK-2091-auto-PR-api-changes
