name: Pull Request Update

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    # TODO: revert to main branch
    uses: viamrobotics/viam-typescript-sdk/.github/workflows/test.yml@RSDK-1843-add-ts-wrapper-for-robot
