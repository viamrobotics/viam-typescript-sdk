name: Test

on:
  workflow_call:

jobs:
  lint_and_build:
    if: github.repository_owner == 'viamrobotics'
    runs-on: [self-hosted, x64]
    container:
      image: ghcr.io/viamrobotics/canon:amd64

    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: |
          # make build
          # sudo mkdir -p "/github/home/.npm"
          # sudo chown -R 1000:1000 "/github/home/.npm"
          sudo chown -R testbot .
          # sudo chown -R 1000:1000 "/github/home/.cache"
          sudo -u testbot bash -lc 'make build'
      #
      # - name: Lint
      #   run: make lint
