{
  "name": "@viamrobotics/sdk-ts-example",
  "version": "0.0.1",
  "description": "quickstart example of a typescript app with viam sdk",
  "scripts": {
    "watch": "esbuild ./src/main.ts --bundle --outfile=static/main.js --servedir=static",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "esbuild": "0.16.12"
  }
}
